import type { GalleryItem } from "./types";

/**
 * Tableau vide tant qu’aucune photo validée (droits + alt) n’est fournie.
 * Aucun placeholder ni image distante.
 */
export const gallery: readonly GalleryItem[] = [];
